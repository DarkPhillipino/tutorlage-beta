node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Separate admin app: private-network-only, not part of the public deploy.
/admin/

# Local tooling state (Claude Code worktrees, Vite dep cache) — not project source.
/.claude/
.vite/
